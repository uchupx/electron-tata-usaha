// import Vue from 'vue'
import { createStore } from 'vuex'
import app from './app'

// Vue.use(Vuex)

export default createStore({
  modules: {
    app,
  },

  state: { // = data

  },

  getters: { // = computed properties

  },

  mutations: {

  }
})